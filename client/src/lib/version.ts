// Human-friendly build label shown in the app (e.g. on the login screen).
// Bump this when shipping a new iPad build so testers can confirm which
// version they're running.
export const APP_BUILD_LABEL = "Alpha 1";
